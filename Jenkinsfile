pipeline {
    agent any  // Adjust if you need specific agents/environments

    stages {
        stage('Run Script') {
            steps {
                script {
                    sh './a.sh'   
                }
            }
        }
    }
}
